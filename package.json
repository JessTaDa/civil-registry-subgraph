{
  "name": "civil-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ jesstada/civil-subgraph",
    "create-local": "graph create --node http://localhost:8020/ jesstada/civil-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ jesstada/civil-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jesstada/civil-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.0",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
